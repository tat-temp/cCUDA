################################################################################
# This .gitignore file was automatically created by Microsoft(R) Visual Studio.
################################################################################

/.vs

# build artifacts (built on the GPU box, not committed)
/CUDACyclone
/CUDACyclone-ptxinfo
/cyclone_tests_results.txt
*.o

# `make gate` scratch log (removed by the target itself; ignored so a failed/interrupted
# run cannot leave an untracked file behind)
/ptxas-gate.log

# importing proof.py from a test script leaves this behind
/__pycache__/

# host field-math equivalence gate (`make fieldtest`): binaries, plus the .inc files
# tests/extract_field.py lifts out of the shipped headers. The .inc files are pure
# derivatives -- regenerated by the target -- so committing them would only let them
# go stale against the headers they are supposed to be testing.
/tests/field_split_equiv
/tests/field_split_include
/tests/field_split_equiv.exe
/tests/field_split_include.exe
/tests/field_extract_rck.inc
/tests/field_extract_split.inc
